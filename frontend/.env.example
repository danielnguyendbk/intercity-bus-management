# Frontend Environment Variables
# Copy file này thành .env.local để override

# Bật/tắt mock mode (true = không cần backend Django, dùng dữ liệu giả)
VITE_USE_MOCK=true

# URL backend Django (chỉ dùng khi VITE_USE_MOCK=false)
VITE_API_BASE_URL=http://localhost:8000/api
